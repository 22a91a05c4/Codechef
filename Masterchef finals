#include <iostream>
using namespace std;
int main()
{
    int a,i;
    cin>>a;
    for(i=1;i<=a;i++)
    {
        int f;
        cin>>f;
        if(f>=1 && f<=10)
        {
            cout<<"YES"<<endl;
        }
        else
        {
            cout<<"NO"<<endl;
        }
    }
	return 0;
}
